# 0.1.0.15：Win11 图标测试候选

2026-09-10，本地构建完成。当时仅构建，未执行安装或发布；随后用户在原沙盒完成 `.14 → .15` 正常覆盖升级，部署日志核验通过，并确认新图标和 ZIP 解压正常。详见[本版沙盒验收](SANDBOX-0.1.0.15-ACCEPTANCE.md)。尚未公开发布。

## 安装包

- 文件：`dist/SmartZipSetup-0.1.0.15-test.exe`
- 大小：15,153,232 字节（14.45 MiB）
- SHA-256：`1AED980A38A21BFC5BC3C92410C5895A2B852F6E2680454AEAECDA9D5F815E2D`
- Publisher：`CN=SmartZip Modern Evaluation`
- 测试签名证书 thumbprint：`A1709CED9DB02150E2FB40CE6496BD1938D89195`

已验收的 `.14` 安装包保留，重新校验 SHA-256 仍为 `C8A9C6F0D3B18D3DFA3D276F3E7C10BD3732771054DF3EED1B8970F5F58594E1`。

## 本次变更

按用户确认的 Win11 图稿进行本地透明背景清理，保留设计，不使用生成工具输出的假棋盘格或毛边版本。复用 System.Drawing 和原构建流程，没有新增软件运行依赖。

- 母版：`packaging/Assets/SmartZip.png`，1254 × 1254、RGBA；SHA-256 `30525DEA3CD9EA7B8D4AF99A9C991B3F430F3B8DD49DFF4339DE95C9AE5AF27D`。
- ICO：16/20/24/32/40/48/64/96/128/256 像素；SHA-256 `0F0F2E6467718761368A37273D965D70A59AE6C485398FE62C9C4B228DB8CED5`。
- 接入 SmartZip EXE、部署助手、测试证书助手、安装器 EXE/向导、三个开始菜单快捷方式，以及 Package 的 44/50/150 像素图标。
- 现代菜单和 AHK 设置窗口继续使用已有的 `Assets/SmartZip.ico` 路径；保留用户配置，不覆盖用户自选图标。
- 不改变 Package 身份、CLSID、解压参数、安装目录规则、回滚流程、更新器或证书权限/所有权逻辑。
- 软件仍 per-user；测试证书仍按原设计仅由最小提权助手处理 LocalMachine\TrustedPeople 公钥信任，不进入 Root。

[设计来源与重建方式](../design/logo/README.md) · [实际 ICO 的明暗底预览](../design/logo/smartzip-win11-icon-preview.png)

## 检查结果

`build.ps1 -Signing Test -Version 0.1.0.15` 完成，退出码 0。构建日志保留于本机 `build/logo-0.1.0.15-build.log`。

- 图标检查通过：原稿内部取样像素未变、外部透明、10 档 ICO 可解码、Package 图片尺寸正确。
- 只读 PE 资源检查通过：四个 EXE 中 10 档图标均与 ICO 原始字节一致；以资源方式加载，没有执行安装器。
- MSIX 文件只读检查通过：版本为 `.15`，Publisher 与 CER Subject 精确匹配；CER 无私钥；内含 PNG/ICO 与构建资源哈希一致。
- 现有目录与中文向导、Inno 提交生命周期、COM 筛选、Unicode 参数、升级/回滚、管道、证书只读自测、发布门禁、离线更新安全测试通过。
- 保留现有两条 C++ `dllexport` 重复声明警告，未借图标工作修改 COM 代码。

以上构建检查不替代真实部署验收。随后 `.14 → .15` 升级已有独立日志，安装向导、开始菜单、一级菜单新图标和一次 ZIP 解压获用户确认；多 DPI、全主题及全部任务栏显示组合未单独覆盖。原安装包哈希不变，构建过程没有自动安装或导入证书。

## 源码提交前复核

2026-09-10 用户完成本版验收后，补记验收与发布文档；没有修改产品运行代码或重建安装器。再次执行中文/目录向导、部署和卸载顺序、更新顺序、发布草稿选择、图标资源、升级备份/恢复决策及沙盒升级入口安全检查，均通过。检查不执行产品安装或修改 Package、证书。

待提交文件清单已排除 `.private`、私钥扩展名、构建/下载缓存与设备日志；已扫描私钥块和常见 GitHub token 模式，无命中。这是指定模式检查，不是完整安全审计。独立文档阅读检查通过，保留用户反馈和日志证据的区别。

本地发布附件中的 `SOURCE-COMMIT.txt` 在提交完成后记录源码 SHA、目标标签与已验收安装包哈希。文档在打包后更新，安装包内的构建时 README 保持原样；不以更新说明为理由替换已验收 EXE。
