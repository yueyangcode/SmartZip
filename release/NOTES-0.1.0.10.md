# SmartZip 0.1.0.10 更新器开发测试版

新增：在使用软件时检查正式 GitHub Release，用户点击「立即更新」后下载、验签并后台升级。支持稍后提醒、跳过版本、下载进度和取消；等待已有解压完成，安装期间阻止新解压，不强制关闭 Explorer 或重启电脑。

复用原覆盖升级/回滚机制；更新结果验证 Package、COM、产品状态和安装收据。拒绝错误来源、哈希、签名和版本，不导入更新证书、不跳过系统信任检查。

用户反馈 0.1.0.9 可以覆盖升级。本版仍为测试签名，离线安全测试不代替真实更新链/窗口验收。尚未公开发布，不支持测试证书到正式证书的自动切换。详见 `docs/UPDATER.md`。
