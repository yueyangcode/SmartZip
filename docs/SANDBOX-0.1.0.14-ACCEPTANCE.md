# 0.1.0.14 沙盒验收进度

更新日期：2026-09-10。用户在沙盒操作界面，开发代理只从共享输出核验证据；不把文件结果、界面反馈和自动化测试互相替代。当前未公开发布。

## 已确认的范围

- 环境：Windows 11 Enterprise 24H2 x64，26100.9278；不是宿主机 Pro 25H2 26200.9278。
- 正常安装包：`dist/SmartZipSetup-0.1.0.14-test.exe`，14,335,128 字节，SHA-256 `C8A9C6F0D3B18D3DFA3D276F3E7C10BD3732771054DF3EED1B8970F5F58594E1`，本轮没有重建或更换安装包。
- 四个故障场景均已验证回滚；`before-commit` 为补充日志复核 PASS，原自动 FAIL 文件保留。第四项 `after-state` 返回预期故障码 20，测试器返回 0，前后快照差异 0，完整恢复健康的 0.1.0.8。
- 随后在同一沙盒正常从 0.1.0.8 升级到 0.1.0.14。日志有 `COMMITTED`、`FINALIZED`；新版健康检查通过，产品状态、卸载项、Package 版本一致，预期 COM 类和现代菜单声明存在。
- 正常升级后，旧 0.1.0.8 文件目录按设计保留到正常卸载；该轮卸载已将两个版本目录移除。后续重装只注册 0.1.0.14 Package。
- 项目证书为无私钥的测试公钥证书。逻辑 CurrentUser TrustedPeople 能继承 LocalMachine 条目，不能把逻辑可见性当作物理重复导入；Root 查询没有项目证书。

## 2026-09-09 升级后的功能验证

| 场景 | 文件证据 | 界面证据 |
| --- | --- | --- |
| ZIP、RAR、7Z | 输出字节数与预期 SHA-256 一致 | 用户随后确认从现代一级菜单执行 |
| 中文、空格、括号、`& % ! ^`、Emoji 路径 | 输入未变，输出内容一致 | 用户确认中文显示和操作正常 |
| ZIP + RAR 多选 | 两个不同预期文本均生成 | 用户确认一级菜单操作 |
| CAB、TAR、GZ、BZ2、GZIP | 输出内容一致 | 用户确认按测试步骤完成 |
| `.001` 第一卷及全部数字分卷多选 | 两组均只生成一个预期文本 | 用户确认按测试步骤完成 |
| 普通文件、文件夹、压缩包混合普通文件 | 过滤组无解压输出；这本身不证明菜单过滤 | 用户明确确认隐藏或禁用 |

12 个正向场景生成 13 个预期文件；23 个样本输入哈希未变，13 个场景目录与采集时一致。DOCX/JPG 是已说明的扩展名过滤占位文件，不是文档/图片打开测试。

GZ/GZIP 样本的归档内文件名本来就是 `内容 __ & % ! ^.txt`，已用后端只读列目录核实；不将其声称为归档内 Emoji 文件名的保真测试。额外顶层 `Cases/01-ZIP.zip` 不属于测试输入，原样保留、未计入结果；不据此判定 SmartZip 生成了额外压缩包。

22:13:30 至 22:19:51 的相关 Application 1000/1001/1002 事件查询成功且无匹配错误。原 Explorer PID 和启动时间保持；另有一个新 Explorer 进程，不能直接认定为崩溃重启。用户确认没有卡顿或界面异常。

## 可追溯证据

均在 `build/sandbox-rollback-0.1.0.14/Output/`，原始机器日志不提交到仓库。

- `rollback-continuation-20260909-212727-after-state/after-state/result.json`：SHA-256 `880A2CC9B506CD5C14853CBEE5FA1F93FB0E6FD18E679CD69025C4F86E7F9BA4`。
- `normal-upgrade-0.1.0.14-first-state/state.json`：SHA-256 `50AB86912C3DC13F19BACC7E726F497AC79BF2042A33606DBB3F05C4804E6915`。20 个导出 helper 日志逐个核验，正常升级段与旧故障测试段分开读取。
- `functional-tests-0.1.0.14/result-a4771f7a14dd4033be8022dbeeea12ce.json`：SHA-256 `97FF3D939B21BB51268503F1CE22C80448666BBABEA0654A600628EBEF91AE39`。
- 随后用户明确确认：一级菜单调用、过滤行为、中文显示、操作流畅度通过。确认不改写原始 JSON 中 `REQUIRES_USER_REPORT` 等采集时状态。

## 2026-09-09 正常卸载验收

22:31:50 至 22:31:58，通过已注册的 `unins000.exe` 正常向导卸载，返回 0。卸载前后证据已核验：

- 程序根目录及 0.1.0.8、0.1.0.14 两个版本目录全部移除；Package、Packaged COM、开始菜单、卸载项和 HKCU 产品状态无残留。
- 本项目创建的 LocalMachine TrustedPeople 测试证书已由正式卸载流程移除，helper 日志记录 `Certificate cleanup: REMOVED`，所有权键也已移除。四个逻辑/物理证书位置检查均无该证书。
- UserData 中的 `SmartZip.ini` 按设计保留：2,412 字节，前后 SHA-256 相同，为 `0822964C2C75FE5B834870DED6699ED65054EFEE296CF4D0096AF767E5AF9B7D`。
- 卸载前后两个 Explorer 进程的 PID、启动时间相同；用户随后确认“智能解压”入口已消失，右键菜单正常。
- Inno 在自身卸载数据文件删除前出现过目录非空 145，正常流程随后重试完成；最终 `Removed all? Yes`、`Need to restart Windows? No`，后快照根目录不存在。不是靠测试器手工清理完成。

证据：`normal-uninstall-0.1.0.14/before.json` SHA-256 为 `5D078BBB77CF942CA4E64925E3F35DF875D1470A3F32CA700908AEC70916E767`；`result.json` 为 `FFF35F8AB4102F4381E4C444D6FF78942D8B472742151B118962D980BF2B164C`。33 个导出 helper 日志哈希核对一致。

`Run-Normal-Uninstall.cmd` 只启动正式卸载程序并采集结果，没有手工删除文件、改证书、补注册、杀进程或自动重试。

## 2026-09-10 卸载后重装

用户在原沙盒运行 `Run-Normal-Reinstall.cmd`，使用同一个正常安装包完成向导。09:20:35 的前快照记录原默认安装目录、产品状态、卸载项、Package、Packaged COM、开始菜单及项目证书均不存在，保留的配置哈希与已审阅卸载结果一致。部署助手另确认本次目标 `C:\SmartZip` 在安装前不存在。

09:21:03 的后快照及日志核验结果：

- 安装器返回 `0`；四个本轮 helper 日志记录 `PREPARED`、`COMMITTED`、`FINALIZED`，Package 健康检查通过，没有手工补注册或清理。
- 实际安装位置为 `C:\SmartZip\Versions\0.1.0.14`，卸载项指向 `C:\SmartZip\unins000.exe`。Inno 记录当前用户安装模式，产品状态和卸载项版本一致，Packaged COM 和开始菜单目录存在。
- 本项目公钥证书重新进入物理 `LocalMachine\TrustedPeople`，无私钥；两个 Root 位置均无该证书。CurrentUser TrustedPeople 仅逻辑可见，物理键不存在。证书创建状态和所有权记录存在。
- `SmartZip.ini` 仍为 2,412 字节，SHA-256 与卸载后相同；两个 Explorer 进程的 PID、启动时间保持。

证据目录：`normal-reinstall-0.1.0.14/`。`before.json` SHA-256 为 `7F20AA67C6E20CBCE767E9C78F3536953FCB7F6C8555B1B68786DDFBF3A9E94E`；`result.json` 为 `4F49B30E7536E0AFE4596593199228C54F04F9C0E35AA781D351253A7F7211B2`。37 个导出 helper 日志均已核验字节数和 SHA-256，其中四个是本轮新增日志。

采集范围限制：脚本的 `RootExists`、`VersionEntries` 固定检查原默认目录，没有跟随本次 `C:\SmartZip` 路径。因此后快照中的 `RootExists=false` 不能作为实际安装失败或实际文件清单通过的证据；本轮未独立采集新目录的逐文件清单和哈希。原始快照保持不变，不补写成 PASS。

随后用户确认“所有的 case 都已经完成，没有问题”。重装后的菜单和功能按用户反馈记为通过；没有再次独立采集逐项输出哈希及功能测试期间的 Event Log，不能复用 9 月 9 日的输出文件证明本轮重新解压成功。无需让用户重复已确认的 case。

本次属于保留配置、正式卸载后换路径重装，不是安装过程中迁移目录，也不是全空用户配置的首次安装。保留第二次安装，不自动卸载或关闭沙盒。

## 后续工作与限制

回滚、覆盖升级、升级后功能、正常卸载及重装部署已有上述证据；重装后功能已有用户通过反馈。下一阶段整理 0.1.0.14 源码及开发版发布材料，另行授权后提交、推送或创建 GitHub 草稿。不要重跑已完成的故障测试、升级或卸载重装流程。

App extensions 设置页仍没有本轮 UI 验收证据；不能为了测试修改 Feature。实际子进程命令行/多选顺序没有现场跟踪，内容检查不能替代参数追踪。沙盒日志记录 `EnableLUA=0`，没有验证普通启用 UAC 的新用户环境。GitHub 提示到静默升级的完整更新链、可信发布签名及公共 WinGet 均未因此通过。
